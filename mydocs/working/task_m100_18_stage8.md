# M100 #18 8단계 완료 보고서

## 단계 목표
- 윈도우 최종 정식 버전인 `v0.1.9`와 완벽히 짝을 맞추는 최신 리눅스 패키지(`.deb`) 빌드를 완수한다.
- 줌 단축키 버그 픽스, 앱 아이콘 교체, HWPX 파일 끌어놓기(D&D) 열기 흐름 등 최신 무결성 소스코드를 온전히 리눅스 바이너리에 탑재한다.

## 변경 및 빌드 내용
- **빌드 환경**: 우분투 리눅스 데스크톱 환경 (x86_64)
- **빌드 커맨드**: `npm run build:linux` (Tauri 패키징 파이프라인 구동)
- **대상 소스**: `local/v0.1.9` 브랜치 (최신 핫픽스 및 코어 안정화 포함)
- **생성된 산출물**:
  - 파일 경로: `/home/joseph/바탕화면/개발/hangul/rhwp-desktop/src-tauri/target/x86_64-unknown-linux-gnu/release/bundle/deb/rhwp_0.1.9_amd64.deb`
  - SHA256 해시: `62bdad6870e39bf2318b3cb053ec257acc16582d0e380dc7b9531781d3f8c11a`

## 검증 계획
1. **로컬 설치 검증**: 기존 하위 버전(`.deb`) 위에 새로운 `.deb` 패키지를 충돌 없이 정상적으로 덮어씌워 업그레이드 설치를 진행한다.
2. **기능 수동 검증**:
   - `Shift+=`를 눌러도 줌 확대 오작동 없이 본문에 정상적으로 `+` 문자가 입력되는지 확인.
   - 단축키 조합을 활용한 본문 줌 확대/축소가 정상적으로 동작하는지 확인.
   - 데스크톱 통합(앱 아이콘 및 리눅스 파일 연동 상태)이 온전히 적용되었는지 확인.

## 판정
- Tauri 기반 리눅스 `v0.1.9` 패키지 빌드 성공 및 무결성 바이너리 완비. (합격)
