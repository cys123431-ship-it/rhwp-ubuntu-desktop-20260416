{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the rhwp desktop shell.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
